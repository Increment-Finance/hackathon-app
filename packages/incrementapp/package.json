{
  "name": "incrementapp",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ chinmaygopal931/incrementapp",
    "create-local": "graph create --node http://localhost:8020/ chinmaygopal931/incrementapp",
    "remove-local": "graph remove --node http://localhost:8020/ chinmaygopal931/incrementapp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 chinmaygopal931/incrementapp"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
